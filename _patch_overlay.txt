/* ── Vidéo arrière-plan — technique robuste ── */
#hero-video {
  position: absolute;
  top: 50%;
  left: 50%;
  min-width: 100%;
  min-height: 100%;
  width: auto;
  height: auto;
  transform: translate(-50%, -50%);
  z-index: 0;
  opacity: 1;
  object-fit: cover;
}

/* Overlay léger — juste assez pour lire le texte */
.hero-overlay {
  position: absolute;
  inset: 0;
  z-index: 1;
  background:
    linear-gradient(180deg, rgba(10, 0, 30, 0.25) 0%, rgba(10, 0, 30, 0.45) 100%);
}